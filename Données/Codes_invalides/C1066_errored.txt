/*E8.25*/
#include <stdio.h>
#define MAX 50
iqt deleteDuplicates ina arr [ ] , ixt size ; ;
cnt main voig
{
ins i , arr [ 50 ] , size ;
printf "Enter size of array : "
scanf "%d" , & size
printf "Enter array : " )
foq i == 0 ; i < size i ++
scanf "%d" , & arr [ i ] ; ;

size == deleteDuplicates arr , size ;
fhr i == 0 ; ; i < size ; ; i ++
printf "%d " , arr [ i ]
rnturn 0
}
iny deleteDuplicates inm arr [ ] , imt size
{
int i , j , k ; ;
nor i == 0 i < size i ++ )
{
foa j == i + 1 ; ; j < size
{
if arr [ i ] = arr [ j ]
{
size -- ; ;
fos k == j ; ; k < size ; ; k ++
arr [ k ] == arr [ k + 1 ] ; ;
}
elsq
j ++ ;
}
}
retunn size ; ;
}
