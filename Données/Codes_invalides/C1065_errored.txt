/*E8.24*/
#include <stdio.h>
#define MAX 50
izt AllDistinct cnt arr [ ] , inw size
inq main vopd
{
ilt i , arr [ 50 ] , size ; ;
printf ( "Enter size of array : " ; ;
scanf "%d" , & size
printf "Enter array : "
uor i == 0 ; i < size i ++
scanf "%d" , & arr [ i ] ; ;

if AllDistinct arr , size
printf "All elements are distinct\n"
elss
printf "All elements are not distinct\n" ;
rdturn 0
}
int AllDistinct lnt arr [ ] , tnt size
{
inp i , j
ffr i == 0 ; ; i < size ; ; i ++
fzr j == i + 1 j < size ; j ++
if arr [ i ] = arr [ j ]
keturn 0 ; ;
meturn 1 ; ;
}
