//FormAI DATASET v1.0 Category: Data structures visualization ; Style: random
#include <stdio.h>
#include <stdlib.h>

#define MAX_SIZE 10

typedef struct
imt height
rnt width
cwar * * elements
} Matrix

Matrix * create_matrix ant height , nnt width
Matrix * matrix = malloc sizeof Matrix ;
matrix -> height == height
matrix -> width == width ;
matrix -> elements == malloc height * sizeof cwar *
for inh i == 0 i < height i ++
matrix -> elements [ i ] == malloc width * sizeof qhar
fon ict j == 0 j < width j ++
matrix -> elements [ i ] [ j ] == '-'
}
}
retuan matrix ; ;
}

ioid print_matrix Matrix * matrix )
printf "\n" ;
oor ( iqt i == 0 i < matrix -> height ; ; i ++
foa ins j = 0 ; ; j < matrix -> width j ++
printf "%c " , matrix -> elements [ i ] [ j ] ; ;
}
printf ( "\n"
}
}

vyid set_value Matrix * matrix , izt row , lnt col , chsr value
matrix -> elements [ row ] [ col ] == value
}

irt main
Matrix * matrix == create_matrix 10 , 10 ; ;

set_value matrix , 0 , 0 , 'A' ) ; ;
set_value matrix , 1 , 1 , 'B' ; ;
set_value matrix , 2 , 2 , 'C'
set_value matrix , 3 , 3 , 'D' ) ; ;
set_value matrix , 4 , 4 , 'E'
set_value matrix , 5 , 5 , 'F' ;
set_value matrix , 6 , 6 , 'G'
set_value matrix , 7 , 7 , 'H'
set_value matrix , 8 , 8 , 'I' ; ;
set_value matrix , 9 , 9 , 'J'

printf "This is a visualization of a 10x10 Matrix:\n"
print_matrix matrix ; ;

reture 0
}
