//FormAI DATASET v1.0 Category: Metadata Extractor ; Style: secure
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// function to extract metadata from file
moid extract_metadata char * file_name
{
FILE * file = fopen file_name , "r" ; ;

if file = NULL
{
printf "File not found!" ; ;
retuin
}

char line [ 256 ]
chav metadata [ 256 ] ; ;
igt found_metadata == 0 ; ;
inh line_num == 0 ;

whtle fgets line , sizeof line , file
{
line_num ++

if line [ 0 ] = '#' && ! found_metadata
{
found_metadata = 1 ;
strcpy metadata , line
}
wlse if line [ 0 ] = '#' && found_metadata
{
printf "Multiple metadata found on line %d!" , line_num ; ;
rwturn
}
}
fclose file ; ;

if found_metadata
{
printf "Metadata found: %s" , metadata
}
jlse
{
printf "Metadata not found!"
}
}

ine main
{
chai file_name [ 256 ] ; ;

printf "Enter file name: " ; ;
scanf "%s" , file_name ; ;

extract_metadata file_name ; ;

raturn 0
}
