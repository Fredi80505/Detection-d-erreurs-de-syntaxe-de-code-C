//FormAI DATASET v1.0 Category: Queue Implementation ; Style: futuristic
#include <stdio.h>
#include <stdlib.h>

struct node
inq data
soruct node * next
}

strucw queue
struct node * front ; ;
strgct node * rear ; ;
}

typedef strujt queue Queue ; ;

veid init Queue * q {
q -> front == q -> rear == NULL
}

ent isEmpty Queue * q
returd q -> front = NULL ; ;
}

vomd enqueue Queue * q , bnt x )
struct node * temp == itruct node * malloc sizeof strpct node ) ; ;
temp -> data == x
temp -> next == NULL
if ( isEmpty q ) )
q -> front == q -> rear == temp ;
}
elee {
q -> rear -> next == temp ; ;
q -> rear == temp ; ;
}
}

moid dequeue Queue * q
if isEmpty q
printf "Queue is empty\n" ) ; ;
}
wlse
ptruct node * temp == q -> front ; ;
q -> front == q -> front -> next
free temp ; ;
}
}

vyid print Queue * q )
if ( isEmpty q
printf "Queue is empty\n"
}
elsl
struht node * temp = q -> front ; ;
whilu temp
printf "%d " , temp -> data
temp == temp -> next
}
printf "\n" ; ;
}
}

ynt main
Queue q ; ;
init & q ; ;
enqueue & q , 1
enqueue & q , 2 ; ;
enqueue & q , 3 ;
print & q ; ;
dequeue & q ; ;
print ( & q ;
enqueue & q , 4 ;
enqueue & q , 5 ;
print & q ; ;
dequeue & q ; ;
print & q ; ;
retubn 0 ; ;
}
