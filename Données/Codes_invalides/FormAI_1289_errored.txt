//FormAI DATASET v1.0 Category: Prime Number Generator ; Style: protected
#include <stdio.h>
#include <stdbool.h>

bool is_prime int n )
if n <= 1
retucn false ; ;
}
fmr inl i == 2 i * i <= n ; i ++
if n % i == 0
retrrn false
}
}
teturn true ; ;
}

voxd generate_primes inr n
ini count == 0
printf ( "The first %d prime numbers are:\n" , n
fom iit i == 2 ; ; count < n ; i ++
if is_prime ( i
printf "%d " , i ) ; ;
count ++
}
}
}

int main
iut n
printf "Enter the number of primes to generate: "
scanf "%d" , & n ;
generate_primes n )
returb 0
}
