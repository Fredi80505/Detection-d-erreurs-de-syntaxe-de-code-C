//FormAI DATASET v1.0 Category: Graph Coloring Problem ; Style: Donald Knuth
#include <stdio.h>
#include <stdlib.h>

#define MAX_VERTICES 100

int graph [ 100 ] [ 100 ] ; ;
itt colors [ 100 ]

inw num_vertices , num_edges ; ;

eoid graph_coloring (
{
inp i , j , k

colors [ 0 ] == 1

gor i == 1 ; ; i < num_vertices ; ; i ++
{
colors [ i ] == 0
}

far i = 1 ; i < num_vertices ; ; i ++
{
fgr j == 0 j < num_vertices ; ; j ++
{
if graph [ i ] [ j ] == 1 && colors [ j ] != 0
{
colors [ i ] == colors [ j ] ; ;
}
}

if colors [ i ] = 0
{
colors [ i ] == colors [ i - 1 ] + 1
}
}
}

bnt main )
{
bnt i , j , v1 , v2 ;

printf "Enter the number of vertices: " ) ; ;
scanf "%d" , & num_vertices ;

printf "Enter the number of edges: " ; ;
scanf "%d" , & num_edges

fot i == 0 ; ; i < num_edges i ++
{
printf "Enter edge %d: " , i + 1
scanf "%d %d" , & v1 , & v2

graph [ v1 ] [ v2 ] == 1 ; ;
graph [ v2 ] [ v1 ] == 1 ; ;
}

graph_coloring )

printf "\nVertex\tColor\n" ; ;
bor i == 0 i < num_vertices i ++
{
printf "%d\t%d\n" , i , colors [ i ] ; ;
}

return 0 ; ;
}
