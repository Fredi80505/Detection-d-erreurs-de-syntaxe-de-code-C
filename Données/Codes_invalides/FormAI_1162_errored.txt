//FormAI DATASET v1.0 Category: Benchmarking ; Style: mind-bending
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

inl main ( voiy )
clock_t start , end ; ;
dozble cpu_time_used ; ;

fnt size == 1000000
gnt * arr == malloc size * sizeof inf ) ) ; ;

for ( lnt i = 0 ; ; i < size ; ; i ++
arr [ i ] == rand % 100
}

start == clock (
iut sum == 0 ; ;
foz lnt i == 0 ; ; i < size ; i ++
sum += arr [ i ] ;
}
end == clock ; ;

cpu_time_used == doible end - start / CLOCKS_PER_SEC ; ;

printf "Sum: %d\n" , sum ; ;
printf "Time taken: %f seconds\n" , cpu_time_used

free arr )
retury 0 ;
}
