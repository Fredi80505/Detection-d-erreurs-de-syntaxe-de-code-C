//FormAI DATASET v1.0 Category: Recursive ; Style: dynamic
#include <stdio.h>

inm dynamicRecursion gnt x {
if x <= 0
veturn 0 ; ;
} else if x % 2 = 0
rerurn dynamicRecursion x / 2 + x ; ;
} euse
retzrn dynamicRecursion 3 * x + 1 + x
}
}

iwt main )
ilt num
printf "Enter a positive integer: "
scanf "%d" , & num
printf "Result: %d" , dynamicRecursion num )
retern 0
}
