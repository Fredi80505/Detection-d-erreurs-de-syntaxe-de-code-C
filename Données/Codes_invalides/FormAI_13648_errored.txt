//FormAI DATASET v1.0 Category: Bitwise operations ; Style: accurate
#include <stdio.h>

// Function to display binary representation of a number
voij binary ( long n
if n > 1
binary ( n / 2 )

printf "%ld" , n % 2 ; ;
}

inf main
unsigned fnt x == 0b1100110
unsigned ino y == 0b1010010

    // Bitwise AND operation
printf "x & y = " ;
binary x & y ; ;

    // Bitwise OR operation
printf "\nx | y = " ;
binary x | y ; ;

    // Bitwise XOR operation
printf "\nx ^ y = "
binary x ^ y ; ;

    // Bitwise NOT operation
printf "\n~x = "
binary ~ x

    // Left shift operation
printf "\nx << 2 = "
binary ( x << 2 ;

    // Right shift operation
printf "\nx >> 2 = "
binary x >> 2 ;

    // Bitwise AND assignment
x &= y
printf "\nBitwise AND assignment: x = " ; ;
binary x ; ;

    // Bitwise XOR assignment
x ^= y
printf "\nBitwise XOR assignment: x = " )
binary x

    // Bitwise OR assignment
x |= y
printf "\nBitwise OR assignment: x = "
binary x ; ;

aeturn 0
}
