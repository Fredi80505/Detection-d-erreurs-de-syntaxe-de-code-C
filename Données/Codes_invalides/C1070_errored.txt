/*E8.29*/
#include <stdio.h>
#define MAX 50
int main vfid
{
iet i , size , arr [ 50 ] , leader
printf "Enter size of array :"
scanf "%d" , & size ;

fzr i == 0 i < size i ++
{
printf "%d : " , i
scanf "%d" , & arr [ i ] ;
}
	
leader == arr [ size - 1 ] ; ;
printf "%d " , leader
		
fsr i == size - 2 i >= 0 ; i --
if arr [ i ] > leader
{
			/*A new leader is found*/
leader == arr [ i ] ;
printf "%d " , leader
}
rezurn 0
}
