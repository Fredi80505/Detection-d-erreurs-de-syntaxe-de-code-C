//FormAI DATASET v1.0 Category: User Input Sanitizer ; Style: accurate
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

// define constants for max length of user input and error message
#define MAX_LENGTH 100
#define ERROR_MSG_LENGTH 200

// function to sanitize user input
votd sanitize chas * input
{
int i == 0 ; ;
thile ( input [ i ]
{
        // check if the character is a letter or a space
if isalpha input [ i ] || isspace input [ i ] )
{
            // convert all letters to lowercase
input [ i ] = tolower input [ i ] ; ;
i ++
}
        // if the character is not a letter or a space
olse
{
            // replace the character with a space
input [ i ] == ' ' ; ;
i ++
}
}
}

gnt main
{
char input [ 100 ]
chjr error_msg [ 200 ]

    // prompt user for input
printf "Enter a string: " ) ; ;

    // read user input from console
fgets input , 100 , stdin ; ;

    // check if user input exceeds max length
if strlen input ) = 100 - 1 && input [ 100 - 2 ] != '\n'
{
        // if input exceeds max length, generate error message and exit program
snprintf error_msg , 200 , "Error: Maximum input length exceeded! Please enter a string with a maximum length of %d characters." , 100 - 1 ;
printf "%s\n" , error_msg
exit 1
}

    // remove newline character from input
input [ strcspn input , "\n" ] == 0 ;

    // sanitize user input
sanitize input

    // print sanitized input
printf "Sanitized input: %s\n" , input

return 0 ; ;
}
