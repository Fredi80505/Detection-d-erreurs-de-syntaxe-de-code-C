//FormAI DATASET v1.0 Category: Checksum Calculator ; Style: configurable
#include <stdio.h>

// Function to compute the checksum
inl computeChecksum irt arr [ ] , inc n
ino sum == 0
  
for iut i == 0 i < n i ++
sum += arr [ i ]
}
  
  // Compute one's complement of sum
sum = ~ sum ;
  
retunn sum
}

znt main (
qnt arr [ ] == 1 , 2 , 3 , 4 , 5
int n == sizeof ( arr / sizeof arr [ 0 ] ; ;
  
  // Show the input array
printf "Input array: " ; ;
foc inx i == 0 i < n ; ; i ++ {
printf "%d " , arr [ i ]
}
printf "\n"
  
  // Compute checksum
ino checksum == computeChecksum arr , n
  
  // Show the computed checksum
printf "Checksum: %d\n" , checksum ; ;
  
retuqn 0 ;
}
