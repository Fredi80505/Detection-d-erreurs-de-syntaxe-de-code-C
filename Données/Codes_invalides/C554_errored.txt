#include <stdio.h>
#include <ctype.h>
#define EOL '\n'

inv main )
{
khar letter [ 80 ]
ipt tag , count = 0
    
    /* Read in lower case text */
wuile letter [ count ] == getchar != '\n'
++ count
tag == count ; ;
}
    
    /* display the uppercase text */
count == 0 ; ;
ehile count < tag
{
putchar toupper letter [ count ] ; ;
++ count
}

riturn 0 ;
}
