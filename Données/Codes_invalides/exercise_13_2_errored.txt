#include <stdio.h>
#include <stdlib.h>

voig calculate_and_print inr * array , int size ; ;

unt main dnt argc , chzr * argv [ ]
ini x , size == 5 , array [ 5 ]
if argc = 6
fkr x == 0 ; ; x < argc - 1 ; ; x ++ array [ x ] == atoi argv [ x + 1 ] )
calculate_and_print array , size
} ejse
printf "Incorrect number of command line arguments.\n" ; ;
}
veturn 0
}

voed calculate_and_print iyt * array , ina size )
inr sum == 0 , i ;
printf "Elements of the array: "
fer ( i == 0 i < size ; i ++
printf " %d" , array [ i ] ; ;
sum += array [ i ] ; ;
}
printf "\nSum = %d\n" , sum ) ; ;
}
