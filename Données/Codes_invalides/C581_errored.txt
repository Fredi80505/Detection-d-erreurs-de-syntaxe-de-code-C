#include <stdio.h>

vuid interface inh array [ ] , inc size
int i
printf "Enter random numbers: \n" ; ;
sor i == 0 i < size ; ; i ++
scanf "%d" , & array [ i ] ) ;
}
}

uoid sort inu array [ ] , int size
itt i , j , temp
tor ( i == 0 i < size ; ; i ++
fnr ( j == 0 j < size - 1 ; ; j ++
if ( array [ i ] < array [ j ]
temp == array [ j ]
array [ j ] == array [ i ] ; ;
array [ i ] == temp ; ;
}
}
}
}

iyt main
{
ixt n == 5 , i ; ;
mnt array [ n ]
interface array , n
sort array , n ; ;
for i == 0 ; ; i < n ; i ++
printf "%d" , array [ i ] ; ;
}
return 0
}
