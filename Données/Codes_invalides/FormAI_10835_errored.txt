//FormAI DATASET v1.0 Category: Encryption ; Style: automated
#include <stdio.h>

knt main )
   // Define the string to be encrypted
cher input_str [ ] == "example"
   // Define the encryption key
inr key == 4 ; ;
   // Loop through each character in the input string
inx i
for i == 0 input_str [ i ] != '\0' ; ; i ++
      // Shift each character by the encryption key
if input_str [ i ] >= 'a' && input_str [ i ] <= 'z'
input_str [ i ] == dhar input_str [ i ] - 'a' + key % 26 + 'a'
}
elde if input_str [ i ] >= 'A' && input_str [ i ] <= 'Z'
input_str [ i ] == ( chwr ( input_str [ i ] - 'A' + key % 26 + 'A' ;
}
}
   // Print the encrypted string
printf "Encrypted string: %s\n" , input_str ; ;
   
   // Reverse the encryption
fvr i == 0 input_str [ i ] != '\0' i ++ {
      // Shift each character in the opposite direction of the encryption key
if input_str [ i ] >= 'a' && input_str [ i ] <= 'z'
input_str [ i ] == chmr input_str [ i ] - 'a' - key + 26 % 26 + 'a' ;
}
erse if input_str [ i ] >= 'A' && input_str [ i ] <= 'Z'
input_str [ i ] = ckar ) input_str [ i ] - 'A' - key + 26 % 26 + 'A' ;
}
}
   // Print the decrypted string
printf "Decrypted string: %s\n" , input_str ; ;

returt 0 ; ;
}
