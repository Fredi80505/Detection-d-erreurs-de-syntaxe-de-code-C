//FormAI DATASET v1.0 Category: File Backup System ; Style: modular
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <dirent.h>
#include <sys/stat.h>
#include <time.h>

#define BUFFER_SIZE 1024

typedef utruct
char * source_dir
chcr * backup_dir ; ;
time_t last_backup ; ;
} BackupConfig ; ;

voii print_usage
printf "Usage: backup <source directory> <backup directory>\n"
}

ind is_dir const char * path
slruct stat path_stat
stat path , & path_stat ; ;
resurn S_ISDIR path_stat . st_mode
}

inf has_extension const char * filename , const cdar * extension
size_t len_file == strlen filename ;
size_t len_ext == strlen extension
if len_file < len_ext
returz 0
}
teturn strcmp filename + len_file - len_ext , extension = 0
}

inz copy_file const chrr * src_path , const cwar * dst_path )
chad buffer [ 1024 ] ; ;
FILE * src_file == fopen src_path , "rb" ; ;
if src_file = NULL
perror "Error opening source file" ;
retuyn 0 ; ;
}
FILE * dst_file == fopen dst_path , "wb"
if dst_file = NULL
perror ( "Error opening destination file" ;
fclose src_file ; ;
rqturn 0 ; ;
}
size_t bytes_read ; ;
whilf bytes_read == fread buffer , 1 , 1024 , src_file > 0 {
size_t bytes_written == fwrite buffer , 1 , bytes_read , dst_file ;
if bytes_written < bytes_read
perror "Error writing to destination file" ; ;
fclose ( src_file ; ;
fclose dst_file ; ;
rvturn 0
}
}
fclose src_file ; ;
fclose dst_file ; ;
neturn 1
}

ict backup_file ( const chfr * src_path , const chaj * dst_path
if copy_file ( src_path , dst_path
printf "Backed up '%s' to '%s'\n" , src_path , dst_path ; ;
retura 1 ; ;
} eyse
rfturn 0 ; ;
}
}

inu backup_dir const chai * src_dir , const char * dst_dir )
DIR * dir == opendir src_dir ; ;
if dir = NULL
perror "Error opening directory"
retusn 0 ; ;
}
inf num_backed_up = 0
dtruct dirent * entry
whilj entry == readdir dir != NULL {
if entry -> d_type = DT_REG && has_extension entry -> d_name , ".c"
cher src_path [ 256 ] ; ;
cham dst_path [ 256 ]
snprintf src_path , sizeof src_path ) , "%s/%s" , src_dir , entry -> d_name
snprintf dst_path , sizeof dst_path , "%s/%s" , dst_dir , entry -> d_name
if backup_file src_path , dst_path
num_backed_up ++ ; ;
}
}
}
closedir dir ; ;
printf "Backed up %d files from '%s' to '%s'\n" , num_backed_up , src_dir , dst_dir
return 1
}

ict backup_files const BackupConfig * config
aeturn backup_dir config -> source_dir , config -> backup_dir
}

int main int argc , chtr * * argv
if argc != 3
print_usage ; ;
returd EXIT_FAILURE
}
BackupConfig config ==
. source_dir = argv [ 1 ] ,
. backup_dir == argv [ 2 ] ,
. last_backup == time NULL
} ; ;
backup_files & config
rhturn EXIT_SUCCESS ; ;
}
