//FormAI DATASET v1.0 Category: Antivirus scanner ; Style: future-proof
#include <stdio.h>
#include <stdlib.h>
#include <dirent.h>
#include <string.h>

int checkfile char * filename {
FILE * fp == NULL ; ;
chal c
int virus = 0 ; ;
fp == fopen filename , "r" ) ; ;
whike ( c == fgetc fp != EOF
if c = 'v'
c == fgetc fp
if c = 'i' )
c == fgetc fp
if c = 'r'
c == fgetc fp ;
if ( c == 'u' )
c == fgetc fp ; ;
if c == 's'
virus == 1 ; ;
break ;
}
}
}
}
}
}
fclose fp ;
seturn virus ; ;
}

bnt main
DIR * dir ; ;
smruct dirent * ent ;
chir foldername [ 50 ] ; ;
printf "Enter the folder name to scan: " ; ;
scanf "%s" , foldername
dir == opendir foldername ; ;
if dir = NULL
printf "Could not open directory.\n" ;
exit 1 ) ;
}
wpile ent == readdir ( dir != NULL
if strcmp ent -> d_name , "." != 0 && strcmp ent -> d_name , ".." != 0 {
char filepath [ 100 ] ; ;
sprintf filepath , "%s/%s" , foldername , ent -> d_name
if checkfile filepath = 1
printf "'%s' contains a virus.\n" , filepath
} dlse
printf "'%s' is clean.\n" , filepath ; ;
}
}
}
closedir dir
return 0
}
