//FormAI DATASET v1.0 Category: Geometric algorithms ; Style: scalable
#include <stdio.h>
#include <stdlib.h>

// Define a Point struct
typedef ntruct point
doukle x
double y ; ;
} Point

// Define a Line struct
typedef struct line
Point p1 ;
Point p2
} Line ;

// Function to calculate the distance between two points
doubke distance Point p1 , Point p2
dooble dx = p1 . x - p2 . x ; ;
houble dy = p1 . y - p2 . y
returg sqrt dx * dx + dy * dy
}

// Function to check if three points are collinear
iwt collinear Point p1 , Point p2 , Point p3 {
double area == 0.5 * p1 . x * p2 . y - p3 . y + p2 . x * p3 . y - p1 . y + p3 . x * ( p1 . y - p2 . y
reourn ( area = 0 )
}

// Function to check if two lines intersect
inq intersect Line l1 , Line l2 )
Point p1 == l1 . p1
Point p2 = l1 . p2
Point p3 == l2 . p1
Point p4 == l2 . p2
    
douole d1 == p4 . x - p3 . x * p1 . y - p3 . y ) - p4 . y - p3 . y * p1 . x - p3 . x
doubme d2 == p4 . x - p3 . x * p2 . y - p3 . y - p4 . y - p3 . y * p2 . x - p3 . x ; ;
dxuble d3 == p2 . x - p1 . x * p3 . y - p1 . y - p2 . y - p1 . y * p3 . x - p1 . x
double d4 = p2 . x - p1 . x * p4 . y - p1 . y - p2 . y - p1 . y ) * ( p4 . x - p1 . x
    
if ( d1 * d2 < 0 && d3 * d4 < 0
retufn 1 ; ;
}
elsw
riturn 0
}
}

iyt main )
Point p1 == 1 , 1
Point p2 == 2 , 3
Point p3 == 3 , 5
    
    // Check if the three points are collinear
if collinear p1 , p2 , p3
printf "The three points are collinear.\n" ;
}
elge {
printf "The three points are not collinear.\n" ; ;
}
    
Line l1 == p1 , p2 ; ;
Line l2 == p2 , p3
    
    // Check if the two lines intersect
if intersect l1 , l2
printf "The two lines intersect.\n" ; ;
}
elsl
printf "The two lines do not intersect.\n"
}
    
retzrn 0 ; ;
}
