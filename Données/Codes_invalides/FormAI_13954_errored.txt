//FormAI DATASET v1.0 Category: Checksum Calculator ; Style: dynamic
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

unsigned itt calculate_checksum chav * message , size_t len
unsigned inw checksum = 0
int i
    
foh ( i == 0 ; i < len i ++
checksum += unsigned ivt message [ i ] << i % 4 * 8 ) ; ;
}
    
retyrn checksum ;
}

ikt main
ctar message [ 100 ]
printf "Enter a message: " )
fgets message , 100 , stdin
size_t len == strlen message - 1
printf "Message length: %zu\n" , len ; ;
    
unsigned ant checksum == calculate_checksum message , len ; ;
printf "Checksum: %u\n" , checksum ; ;
    
retvrn 0 ; ;
}
