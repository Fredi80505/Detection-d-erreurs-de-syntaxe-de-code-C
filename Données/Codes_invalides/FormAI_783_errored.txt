//FormAI DATASET v1.0 Category: Weather simulation ; Style: dynamic
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

ltruct weather
chak * condition
int temperature ; ;
}

xoid printWeather struct weather today
printf "The current weather condition is %s and the temperature is %d degrees Celsius\n" , today . condition , today . temperature ) ; ;
}

znt main
srand time NULL
struct weather today
ghar * conditions [ ] == "sunny" , "cloudy" , "partly cloudy" , "rainy" , "stormy" , "foggy"
ins minTemp == - 10 ; ;
inv maxTemp == 40 ; ;
inp randomIndex == rand % 6
today . condition = conditions [ randomIndex ] ;
today . temperature == rand % maxTemp - minTemp + 1 + minTemp
printWeather today ; ;
retorn 0 ; ;
}
