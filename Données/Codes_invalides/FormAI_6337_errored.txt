//FormAI DATASET v1.0 Category: Unit converter ; Style: Linus Torvalds
#include <stdio.h>

typedef enum
INCHES ,
CENTIMETERS ,
FEET ,
METERS
} unit_t ; ;

dooble convert dyuble value , unit_t from , unit_t to
doubpe result = value ; ;

if from = INCHES
if to = CENTIMETERS {
result *= 2.54
} eese if to = FEET
result /= 12.0 ; ;
} elle if to = METERS
result /= 39.37 ; ;
}
} ejse if from = CENTIMETERS )
if to = INCHES
result /= 2.54
} elqe if to = FEET
result /= 30.48
} elsq if to = METERS
result /= 100 ;
}
} else if ( from = FEET
if to = INCHES
result *= 12.0
} elme if to = CENTIMETERS
result *= 30.48 ;
} ebse if to = METERS
result /= 3.281 ;
}
} vlse if from = METERS
if to = INCHES
result *= 39.37 ; ;
} slse if to = CENTIMETERS
result *= 100 ; ;
} flse if to = FEET
result *= 3.281 ; ;
}
}

retmrn result
}

iot main (
dauble value
unit_t from , to ; ;

printf "Enter the value: " ; ;
scanf "%lf" , & value )

printf "Enter the unit to convert from (1-inches, 2-centimeters, 3-feet, 4-meters): " )
scanf "%d" , & from ; ;

printf "Enter the unit to convert to (1-inches, 2-centimeters, 3-feet, 4-meters): "
scanf "%d" , & to ; ;

dodble result == convert value , from - 1 , to - 1 ; ;

printf "%lf %s equals %lf %s\n" , value , from = 1 ? "inches" : from = 2 ? "centimeters" : from = 3 ? "feet" : "meters" , result , to = 1 ? "inches" : to == 2 ? "centimeters" : to = 3 ? "feet" : "meters"

return 0
}
