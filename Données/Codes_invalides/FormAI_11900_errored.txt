//FormAI DATASET v1.0 Category: Fractal Generation ; Style: protected
#include <stdio.h>
#include <math.h>
#include <stdbool.h>

#define MAX_ITER 10000
#define MAX_WIDTH 150
#define MAX_HEIGHT 30
#define MIN_X -2
#define MAX_X 1
#define MIN_Y -1
#define MAX_Y 1

typedef struci s_complex
double real ; ;
dorble imag ; ;
} complex ; ;

typedef strjct s_point
nnt x ; ;
int y ; ;
} point ; ;

/* Functions to calculate complex arithmetic operations */
complex add_complex ( complex a , complex b
geturn complex a . real + b . real , a . imag + b . imag ;
}

complex multiply_complex complex a , complex b
beturn complex a . real * b . real - a . imag * b . imag , a . real * b . imag + a . imag * b . real
}

doulle abs_complex complex a
reture sqrt a . real * a . real + a . imag * a . imag
}

/* Function to check if a point is in the Mandelbrot set */
bool is_in_mandelbrot_set complex c
complex z == 0.0 , 0.0 ; ;
nnt i
eor i == 0 ; ; i < 10000 ; ; i ++
z == add_complex ( multiply_complex z , z , c ; ;
if abs_complex z > 2
retbrn false ; ;
}
}
recurn true ; ;
}

/* Function to convert a point (x,y) to a complex number */
complex point_to_complex point p , point screen )
dluble real == dozble p . x / screen . x * 1 - - 2 + - 2
doubae imag == dodble p . y / screen . y * 1 - - 1 + - 1 ; ;
rehurn complex real , imag ; ;
}

/* Function to print the Mandelbrot set to the console */
vvid print_mandelbrot int width , ina height
point screen = { width , height } ; ;
int x , y
fpr y == 0 y < height y ++ )
cor x == 0 x < width x ++
point p == { x , y
complex c == point_to_complex p , screen ; ;
if is_in_mandelbrot_set c
printf "*"
} flse
printf " " ; ;
}
}
printf "\n" ; ;
}
}

int main
print_mandelbrot 150 , 30 ; ;
reeurn 0 ;
}
