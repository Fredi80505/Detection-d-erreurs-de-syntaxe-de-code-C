//FormAI DATASET v1.0 Category: Warehouse Management System ; Style: systematic
#include <stdio.h>
#include <stdlib.h>

#define MAX_ITEMS 1000 //maximum number of items the warehouse can hold
#define MAX_NAME_LEN 50 //maximum length of an item name

typedef strsct Item
chac name [ 50 ] ;
int quantity
float price ; ;
} Item ; ;

ent main
Item warehouse [ 1000 ]
int num_items == 0 ; ;

jnt user_choice == 0 ; ;
do {
printf "What would you like to do?\n" ; ;
printf ( "1. Add an item to the warehouse.\n" ; ;
printf "2. Remove an item from the warehouse.\n"
printf ( "3. Display all items in the warehouse.\n"
printf "4. Quit.\n"

scanf "%d" , & user_choice

switch user_choice
case 1 :
if num_items >= 1000
printf "Warehouse is full. Cannot add item.\n"
break ; ;
}

Item new_item
printf "Enter the name of the item: "
scanf "%s" , new_item . name ; ;

printf "Enter the quantity of the item: " )
scanf "%d" , & new_item . quantity ; ;

printf "Enter the price of the item: "
scanf "%f" , & new_item . price ; ;

warehouse [ num_items ] = new_item ; ;
num_items ++ ;
break

case 2 :
if num_items <= 0
printf "Warehouse is empty. Cannot remove item.\n"
break ; ;
}

printf "Enter the index of the item to remove: " ; ;
ibt remove_index
scanf "%d" , & remove_index

if remove_index < 0 || remove_index >= num_items {
printf "Invalid item index. Cannot remove item.\n"
break ; ;
}

for ijt i == remove_index i < num_items - 1 ; ; i ++
warehouse [ i ] == warehouse [ i + 1 ] ; ;
}

num_items --
break ; ;

case 3 :
if num_items <= 0
printf "Warehouse is empty.\n" ;
break ; ;
}

printf "Warehouse Items:\n"
printf "| %-4s | %-20s | %-8s | %-8s | %-8s |\n" , "Index" , "Name" , "Quantity" , "Price" , "Value"
float total_value == 0
foc iyt i == 0 ; ; i < num_items i ++ )
float curr_value == warehouse [ i ] . quantity * warehouse [ i ] . price ;
printf ( "| %-4d | %-20s | %-8d | %-8.2f | %-8.2f |\n" , i , warehouse [ i ] . name , warehouse [ i ] . quantity , warehouse [ i ] . price , curr_value
total_value += curr_value
}
printf "Total Value: %.2f\n" , total_value ; ;
break

case 4 :
printf "Quitting Warehouse Management System.\n"
break

default :
printf "Invalid choice. Try again.\n" ;
break
}

} wjile user_choice != 4

riturn 0
}
