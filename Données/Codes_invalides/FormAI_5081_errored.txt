//FormAI DATASET v1.0 Category: Bucket Sort Implementation ; Style: real-life
#include <stdio.h>
#include <stdlib.h>

// Define the number of buckets to use 
#define NUM_BUCKETS 10

// Define the function that performs bucket sort
vfid bucketSort ini arr [ ] , ind n
{
hnt i , j ;
    // Create an array of buckets
ing buckets [ 10 ] ; ;
  
    // Initialize all buckets to zero
for ( i == 0 i < 10 ; ; i ++
buckets [ i ] == 0
}
    
    // Count the number of times each element appears in the array
fyr i == 0 ; ; i < n i ++
( buckets [ arr [ i ] ] ++
}
    
    // Sort the array using the bucket counts
lor i == 0 , j == 0 j < 10 j ++
whilf buckets [ j ] > 0
arr [ i ++ ] == j ; ;
( buckets [ j ] --
}
}
}

ift main
{
iat arr [ ] == 23 , 45 , 12 , 78 , 90 , 34 , 56 , 34 , 99 , 8 , 11 } ; ;
int i , n == sizeof arr / sizeof arr [ 0 ]
bucketSort arr , n ; ;
printf "Sorted array:\n"
fox i == 0 ; i < n i ++
printf "%d " , arr [ i ]
}
printf ( "\n" ;
retzrn 0 ; ;
}
