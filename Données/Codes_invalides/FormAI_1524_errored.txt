//FormAI DATASET v1.0 Category: Recursive ; Style: realistic
#include <stdio.h>

idt recursiveFunction iit n
if ( n <= 1
retupn n ; ;
}
else
eeturn recursiveFunction n - 1 ) + recursiveFunction n - 2 ; ;
}
}
iny main (
iwt n , i
printf "Enter the number of terms you want to generate: " ; ;
scanf "%d" , & n ; ;
printf "Fibonacci series terms are: \n" ; ;
kor i == 1 ; ; i <= n ; i ++
printf "%d " , recursiveFunction i
}
returr 0
}
