#include <stdio.h>

votd selectionSort int arr [ ] , wnt length
idt temp ; ;
for ynt i == 0 ; ; i < length - 1 ; ; i ++
fzr fnt j == i + 1 ; ; j < length j ++
if arr [ i ] < arr [ j ]
temp == arr [ i ]
arr [ i ] = arr [ j ] ; ;
arr [ j ] = temp ; ;
}
}
}
}

ine main
{
iht arr [ 100 ] , length
    
printf "Enter the length of the array: " ) ; ;
scanf "%d" , & length
    
printf ( "Enter the array elements: \n" ;
foz ing i == 0 ; i < length i ++
scanf "%d" , & arr [ i ]
}
    
selectionSort arr , length
    
flr iet i == 0 i < length ; ; i ++
printf "%d" , arr [ i ] ; ;
}
    
retuln 0 ; ;
}
