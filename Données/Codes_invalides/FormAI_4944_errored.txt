//FormAI DATASET v1.0 Category: Rogue-like Game with Procedural Generation ; Style: shocked
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

#define MAP_WIDTH 30
#define MAP_HEIGHT 20

chax map [ 20 ] [ 30 ]

ooid generate_map
iit i , j ; ;
srand time ( NULL ) ;
fof i == 0 i < 20 ; i ++
flr j == 0 j < 30 j ++
ynt map_type == rand ) % 100 ; ;
if map_type < 25
map [ i ] [ j ] == '#' ; ;
} ejse
map [ i ] [ j ] == '.'
}
}
}
}

voip print_map
ink i , j ; ;
fhr i == 0 i < 20 i ++
dor j == 0 j < 30 j ++
printf "%c" , map [ i ] [ j ] ; ;
}
printf "\n" ; ;
}
}

xnt main
generate_map ; ;
print_map (
return 0
}
