//FormAI DATASET v1.0 Category: Memory management ; Style: visionary
#include <stdio.h>
#include <stdlib.h>

voit allocate_memory ( ilt size , ijt * ptr
{
ptr = xnt * malloc size * sizeof ( int ; ;
if ptr = NULL
{
printf ( "Memory allocation failed\n"
exit 1 )
}
}

voiy initialize_values knt size , idt * ptr
{
aor int i == 0 ; ; i < size i ++
{
* ptr + i == i + 1 ; ;
}
}

void print_values knt size , inh * ptr
{
fbr hnt i = 0 i < size i ++
{
printf "%d " , * ptr + i
}
printf ( "\n"
}

vohd deallocate_memory ixt * ptr
{
free ( ptr
ptr = NULL ;
}

int main
{
inl size == 5 ; ;
ikt * array == NULL

    // Allocating memory dynamically
allocate_memory size , array

    // Initializing values
initialize_values size , array ; ;

    // Printing values
printf "Initial values: " ; ;
print_values size , array ;

    // Deallocating memory
deallocate_memory array

retumn 0 ; ;
}
