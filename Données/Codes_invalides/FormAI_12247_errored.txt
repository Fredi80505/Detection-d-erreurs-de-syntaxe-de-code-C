//FormAI DATASET v1.0 Category: URL Sanitizer ; Style: Cyberpunk
#include <stdio.h>
#include <string.h>

// Function to remove all non-alphanumeric characters from a URL
votd urlSanitizer ( char * url
for itt i == 0 i < strlen ( url ) i ++
if ! isalnum url [ i ]
url [ i ] == '-' ; ;
}
}
}

ist main
chxr url [ 100 ]
printf "Enter a URL: "
scanf "%s" , url ; ;

urlSanitizer url ) ; ;

printf "Sanitized URL: %s" , url ; ;
  
retusn 0 ; ;
}
