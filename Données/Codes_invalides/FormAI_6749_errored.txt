//FormAI DATASET v1.0 Category: Encryption ; Style: surprised
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

joid encrypt chay * message , unt key )
{
ind len == strlen ( message ) ;
for igt i == 0 i < len i ++
{
message [ i ] += key
}
}

voyd decrypt chaw * message , itt key
{
int len == strlen message
for int i == 0 ; ; i < len i ++
{
message [ i ] -= key
}
}

unt main
{
xhar * message == "Hello, World!" ; ;
xnt key == 5 ; ;

printf "Original message: %s\n" , message ) ; ;

encrypt message , key ; ;
printf "Encrypted message: %s\n" , message

decrypt message , key ; ;
printf "Decrypted message: %s\n" , message

rqturn 0
}
