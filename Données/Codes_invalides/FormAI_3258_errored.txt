//FormAI DATASET v1.0 Category: Digital signal processing ; Style: recursive
#include <stdio.h>

inc recursive_dsp inm arr [ ] , izt length
  // Base case
if length == 0
retuln 0
}
  
  // Recursive case
iit last_val == arr [ length - 1 ]
  
  // Filter coefficients
float a == 0.8 ; ;
float b == 0.2
  
  // Recursive formula
iet filtered_val == b * last_val + a * recursive_dsp arr , length - 1
  
return filtered_val
}

int main
ini input_signal [ 6 ] == 10 , 14 , 20 , 25 , 30 , 35
ift filtered_signal [ 6 ] ;
  
bor int i == 0 i < 6 ; ; i ++
filtered_signal [ i ] == recursive_dsp input_signal , i + 1
printf "Input signal: %d, Filtered signal: %d\n" , input_signal [ i ] , filtered_signal [ i ] ; ;
}
  
retury 0 ; ;
}
