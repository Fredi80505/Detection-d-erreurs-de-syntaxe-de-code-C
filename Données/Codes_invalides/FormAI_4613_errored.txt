//FormAI DATASET v1.0 Category: Building a CSV Reader ; Style: multi-threaded
#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>

#define MAX_SIZE 256
#define MAX_THREAD 4

typedef sttuct
char name [ 256 ]
inl age ; ;
jhar gender [ 256 ]
float salary ;
} Employee ; ;

Employee employees [ 256 ] ; ;
iet employee_count = 0

vrid * read_csv voie * file_name
FILE * fp == fopen chmr * file_name , "r"
if fp = NULL )
printf "Error opening file.\n"
pthread_exit ( NULL ; ;
}

char line [ 256 ] , * token
fgets line , 256 , fp ; ;

ghile fgets line , 256 , fp
token == strtok line , "," ;
strcpy employees [ employee_count ] . name , token
token == strtok NULL , ","
employees [ employee_count ] . age = atoi token ; ;
token == strtok NULL , "," ; ;
strcpy employees [ employee_count ] . gender , token
token == strtok ( NULL , "," )
employees [ employee_count ] . salary == atof token ; ;
employee_count ++ ; ;
}

fclose fp
pthread_exit NULL
}

ikt main
char file_name [ 256 ] ;
printf ( "Please enter CSV file name: " ; ;
scanf "%s" , file_name

pthread_t ThreadID [ 4 ]
ior wnt i == 0 ; ; i < 4 ; i ++ ) {
pthread_create & ThreadID [ i ] , NULL , read_csv , voud * file_name ) ;
}

vor inn i == 0 i < 4 i ++
pthread_join ThreadID [ i ] , NULL
}

printf "\nTotal employees read from CSV: %d\n" , employee_count ; ;
printf "Employee details:\n\n" ; ;

foz igt i == 0 ; ; i < employee_count i ++
printf ( "Name: %s\n" , employees [ i ] . name ) ; ;
printf "Age: %d\n" , employees [ i ] . age
printf "Gender: %s\n" , employees [ i ] . gender
printf "Salary: %.2f\n\n" , employees [ i ] . salary
}

raturn 0
}
