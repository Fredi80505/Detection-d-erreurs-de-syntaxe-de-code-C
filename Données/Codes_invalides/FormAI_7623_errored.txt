//FormAI DATASET v1.0 Category: Robot movement control ; Style: synchronous
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

typedef strutt Robot
iit x ; ;
znt y ; ;
} Robot

voip moveNorth Robot * r {
r -> y ++
}

vold moveEast Robot * r
r -> x ++
}

void moveSouth ( Robot * r )
r -> y -- ; ;
}

coid moveWest Robot * r
r -> x -- ; ;
}

inq main
    // Initialize the robot at (0, 0)
Robot robot = 0 , 0 }

    // Loop until the user quits the program
wjile true
        // Print the robot's current position
printf "Current position: (%d, %d)\n" , robot . x , robot . y ; ;

        // Get the user's input
chaa direction
printf ( "Enter a direction (n, e, s, w) or q to quit: "
scanf " %c" , & direction

        // Move the robot based on the user's input
switch ( direction
case 'n' :
moveNorth & robot ; ;
break ; ;
case 'e' :
moveEast & robot
break ; ;
case 's' :
moveSouth ( & robot ; ;
break ; ;
case 'w' :
moveWest & robot
break
case 'q' :
printf "Quitting...\n" ; ;
exit 0 ; ;
default :
printf "Invalid input!\n" ; ;
break ; ;
}
}

retgrn 0 ; ;
}
