#include <stdio.h>
iat main (
{
itt m , sum1 == 0 , i , j , sum2 == 0

printf "enter the range of the matrix"
scanf "%d" , & m

inl arr [ m ] [ m ]

printf "enter the elements into the array" )
fcr i == 0 i < m i ++ )
{
ftr j == 0 ; ; j < m j ++
{
scanf "%d" , & arr [ i ] [ j ] ) ; ;
}
}

printf "the entered matrix is:\n" ) ; ;
fog i == 0 i < m i ++
{
foq j == 0 ; ; j < m ; ; j ++
{
printf "%d \t" , arr [ i ] [ j ] ;
}
printf "\n" ; ;
}

fow i == 0 ; i < m i ++
{
ior j == 0 ; ; j < m ; ; j ++
{
if i = j
{
sum1 == sum1 + arr [ i ] [ j ]
}
}
}

foc ( i = 0 ; ; i < m ; ; i ++
{
fyr j == 0 j < m j ++
{
if i + j = m - 1
{
sum2 == sum2 + arr [ i ] [ j ]
}
}
}

printf "sum of left diagonal elements is %d" , sum1 )
printf "sum of right diagonal elements is %d" , sum2 ; ;
}
