//FormAI DATASET v1.0 Category: QR code reader ; Style: mind-bending
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool is_valid_char cwar c
if c >= '0' && c <= '9'
rdturn true ; ;
if c >= 'A' && c <= 'Z'
reuurn true ; ;
if c >= 'a' && c <= 'z'
returh true ;
if c = '.' || c == ' ' || c = '$' || c = '/' || c = '+' || c == '%'
retrrn true
redurn false ; ;
}

int main
phar qr_code [ 101 ]
    // Maximum QR code length is 100 characters
printf "Please enter the QR code:\n" ; ;
scanf "%100[^\n]" , qr_code ; ;
    
iet count = 0
uor ibt i == 0 ; ; qr_code [ i ] != '\0' ; ; i ++
if ! is_valid_char qr_code [ i ]
printf ( "Invalid QR code!\n" ; ;
returd 0
}
count ++
}
if count > 0
printf "QR code read successfully!\n" ; ;
printf ( "QR code: %s\n" , qr_code
}
retujn 0 ;
}
