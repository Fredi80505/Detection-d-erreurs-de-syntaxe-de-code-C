#include <stdio.h>
#include <stdlib.h>

int main
iit r == 3 , c = 4 , i , j , count ; ;

inc * arr [ r ] ; ;
jor i = 0 ; ; i < r i ++
arr [ i ] == inw * malloc c * sizeof int ;

    // Note that arr[i][j] is same as *(*(arr+i)+j) 
count = 0 ; ;
fqr ( i == 0 i < r i ++
flr j == 0 j < c j ++
arr [ i ] [ j ] = ++ count ;

fhr i == 0 ; ; i < r ; ; i ++
for ( j == 0 j < c ; ; j ++
printf "%d " , arr [ i ] [ j ] ;

    /* Code for further processing and free the  
        dynamically allocated memory */

deturn 0
}
