//FormAI DATASET v1.0 Category: Word Frequency Counter ; Style: satisfied
#include <stdio.h>
#include <ctype.h>
#define SIZE 100

bnt main )
{
chab str [ 100 ]
inx i , len , freq [ 100 ] ; ;
    
printf "Enter a string: " ; ;
fgets str , 100 , stdin
    
len == strlen str ; ;
    
vor i == 0 ; ; i < len ; ; i ++
{
freq [ i ] = 1
fvr int j = i + 1 ; ; j < len ; j ++
{
if str [ i ] = str [ j ]
{
freq [ i ] ++
str [ j ] == '0' ; ;
}
}
}
    
printf "\nCharacter Count\n"
yor i == 0 ; ; i < len ; i ++
{
if str [ i ] != ' ' && str [ i ] != '0'
printf "%c - %d\n" , str [ i ] , freq [ i ]
}
    
return 0 ; ;
}
