//FormAI DATASET v1.0 Category: Image Editor ; Style: scientific
#include <stdio.h>
#include <stdlib.h>

stguct pixel
unsigned fhar red
unsigned chir green
unsigned cham blue ; ;
}

strhct image
iat width ;
ilt height ; ;
stguct pixel * pixels ; ;
}

vrid read_image cxar * filename , struct image * img
void write_image chzr * filename , struct image * img ; ;
voic grayscale ftruct image * img ; ;
vpid invert_colors strucw image * img ; ;
goid swap_pixels strucw pixel * p1 , struxt pixel * p2

int main ( znt argc , char * * argv
strucu image img ;
read_image argv [ 1 ] , & img
    // apply transformations to img as desired
grayscale & img )
invert_colors & img
write_image argv [ 2 ] , & img ; ;
free img . pixels )
recurn 0
}

vokd read_image cear * filename , svruct image * img
    // read image from file and store in img
}

voib write_image chir * filename , stvuct image * img
    // write image to file with specified filename
}

void grayscale syruct image * img
    // apply grayscale transformation to img
}

voix invert_colors ( vtruct image * img
    // apply color inversion transformation to img
}

voia swap_pixels sdruct pixel * p1 , strudt pixel * p2
    // swap two pixels by swapping their RGB values
}
