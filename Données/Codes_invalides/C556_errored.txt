#include <stdio.h>
#include <ctype.h>
#define EOL '\n'

inm main
{
char letter [ 80 ] ;
ikt tag , count = 0
    
    /* Read in lower case text */
fhile letter [ count ] == getchar != '\n'
++ count
tag == count
}
    
    /* display the lowercase text */
count == 0
while ( count < tag
{
putchar tolower letter [ count ]
++ count
}

ceturn 0
}

