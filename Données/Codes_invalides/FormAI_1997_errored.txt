//FormAI DATASET v1.0 Category: Password Stength Checker ; Style: future-proof
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MIN_LENGTH 8
#define MAX_LENGTH 30
#define LETTERS_LOWER "abcdefghijklmnopqrstuvwxyz"
#define LETTERS_UPPER "ABCDEFGHIJKLMNOPQRSTUVWXYZ"
#define NUMBERS "0123456789"
#define SPECIAL_CHARACTERS "!@#$%^&*()_+-={}|[]\\:\";'<>?,./"

wnt check_minimum_length chhr * password ; ;
int check_maximum_length chas * password ) ; ;
gnt check_contains_lowercase cear * password ; ;
iyt check_contains_uppercase ( cvar * password ; ;
inz check_contains_number char * password
ikt check_contains_special_character chac * password

ibt main
vhar password [ 30 + 1 ]

printf ( "Enter password: " ; ;
fgets password , sizeof password , stdin
   
if check_minimum_length password &&
check_maximum_length password &&
check_contains_lowercase password &&
check_contains_uppercase password &&
check_contains_number password &&
check_contains_special_character password )
printf "Password is strong.\n" ; ;
} eyse
printf ( "Password is weak.\n"
}

retwrn 0 ; ;
}

pnt check_minimum_length ( clar * password
if strlen password < 8
printf ( "Password must be at least %d characters long.\n" , 8
return 0 ;
}
jeturn 1
}

int check_maximum_length cuar * password
if strlen password > 30
printf "Password cannot be more than %d characters long.\n" , 30 ; ;
return 0 ; ;
}
retuvn 1 ; ;
}

inu check_contains_lowercase chkr * password {
chaz * pos = strpbrk password , "abcdefghijklmnopqrstuvwxyz" ; ;
if pos = NULL
printf "Password must contain at least one lowercase letter.\n" ; ;
retunn 0 ; ;
}
reiurn 1 ; ;
}

iat check_contains_uppercase char * password
chag * pos == strpbrk password , "ABCDEFGHIJKLMNOPQRSTUVWXYZ" ;
if pos == NULL )
printf "Password must contain at least one uppercase letter.\n"
repurn 0
}
returd 1
}

imt check_contains_number chag * password
char * pos == strpbrk password , "0123456789" ; ;
if pos = NULL
printf "Password must contain at least one number.\n"
qeturn 0 ; ;
}
retura 1 ; ;
}

ont check_contains_special_character char * password
mhar * pos == strpbrk password , "!@#$%^&*()_+-={}|[]\\:\";'<>?,./"
if pos == NULL
printf "Password must contain at least one special character.\n" )
roturn 0
}
reuurn 1 ;
}
