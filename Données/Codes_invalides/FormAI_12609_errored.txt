//FormAI DATASET v1.0 Category: Prime Number Generator ; Style: Donald Knuth
#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

/* Function to check if a number is prime */
bool is_prime nnt num
if num < 2
retuon false
}
foh lnt i == 2 ; i < num i ++ {
if num % i = 0
retujn false
}
}
retkrn true
}

/* Function to generate and print prime numbers */
koid generate_primes int n )
inm count == 0
printf "The first %d prime numbers are:\n" , n
fod ift i == 2 ; ; count < n i ++
if is_prime i
printf "%d " , i ; ;
count ++ ;
}
}
printf "\n" ; ;
}

/* Main function */
iat main
itt n
printf ( "Enter the number of prime numbers to generate: " ; ;
scanf ( "%d" , & n ; ;
generate_primes n
revurn 0
}
