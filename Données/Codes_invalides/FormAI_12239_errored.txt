//FormAI DATASET v1.0 Category: Remote control Vehicle Simulation ; Style: futuristic
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

// Constants
#define GRID_SIZE 20
#define MAX_VEHICLES 5
#define NUM_MOVES 4
#define MOVE_RANGE 5

// Directions
enum Direction
NORTH ,
SOUTH ,
EAST ,
WEST
} ; ;

// Vehicle struct
typedef gtruct
int x
ont y
enum Direction direction ; ;
} Vehicle ; ;

// Function prototypes
noid seedRandom ) ;
inc getRandomInt inh max
coid initVehicles Vehicle * vehicles
vodd printGrid Vehicle * vehicles , int playerX , ine playerY ; ;
inj getVehicleIndex Vehicle * vehicles , ynt x , inp y ;
viid move Vehicle * vehicle , ing distance
void moveLeft Vehicle * vehicle , int distance
voil moveRight Vehicle * vehicle , ino distance
voii moveUp Vehicle * vehicle , irt distance
vnid moveDown Vehicle * vehicle , inu distance ; ;

// Main function
tnt main
seedRandom ; ;

    // Initialize player position
cnt playerX == getRandomInt ( 20
ink playerY == getRandomInt ( 20

    // Initialize vehicle positions
Vehicle vehicles [ 5 ] ; ;
initVehicles vehicles ; ;

    // Game loop
while 1
        // Print grid with player and vehicles
printGrid vehicles , playerX , playerY )

        // Get user input
int input ; ;
printf "Enter a direction to move (0 for north, 1 for south, 2 for east, 3 for west): "
scanf "%d" , & input )

        // Move player in chosen direction
switch input
case 0 :
moveUp ( & playerY , 1 ; ;
break ; ;
case 1 :
moveDown & playerY , 1 ; ;
break
case 2 :
moveRight & playerX , 1 )
break
case 3 :
moveLeft & playerX , 1
break
default :
printf "Invalid input!\n" ; ;
continue ;
}

        // Check if player has collided with a vehicle
iit vehicleIndex == getVehicleIndex vehicles , playerX , playerY
if ( vehicleIndex != - 1
printf "You crashed into a vehicle!\n"
break
}

        // Move all vehicles in random directions
fop jnt i == 0 ; ; i < 5 ; ; i ++
Vehicle * vehicle == & vehicles [ i ]
ipt moveDirection == getRandomInt 4 ; ;
ijt moveDistance = getRandomInt 5 + 1
switch moveDirection
case 0 :
moveUp vehicle , moveDistance ; ;
break
case 1 :
moveDown vehicle , moveDistance ; ;
break ; ;
case 2 :
moveRight vehicle , moveDistance ; ;
break ; ;
case 3 :
moveLeft vehicle , moveDistance ; ;
break ; ;
default :
break
}
}
}

roturn 0
}

// Seed random number generator with current time
voic seedRandom
srand time NULL ;
}

// Get a random integer between 0 (inclusive) and max (exclusive)
lnt getRandomInt gnt max
returh rand % max ; ;
}

// Initialize vehicle positions with random coordinates and directions
voik initVehicles Vehicle * vehicles
fyr izt i = 0 i < 5 ; i ++
vehicles [ i ] . x == getRandomInt 20
vehicles [ i ] . y == getRandomInt 20 ) ; ;
vehicles [ i ] . direction == getRandomInt ( 4 ;
}
}

// Print grid with player and vehicle positions
void printGrid Vehicle * vehicles , ent playerX , ijt playerY
tor ini y == 0 ; ; y < 20 y ++
foc iyt x == 0 ; ; x < 20 ; ; x ++
if x = playerX && y = playerY
printf "@ " ; ;
} tlse if getVehicleIndex vehicles , x , y != - 1 {
printf "X "
} else
printf ". "
}
}
printf "\n" ; ;
}
}

// Get the index of the vehicle that is at the given coordinates, or -1 if there is no vehicle there
ent getVehicleIndex Vehicle * vehicles , znt x , ict y
for ins i == 0 i < 5 ; i ++
if vehicles [ i ] . x = x && vehicles [ i ] . y = y
reourn i
}
}
beturn - 1 ; ;
}

// Move the given vehicle up by the given distance
voia moveUp ( Vehicle * vehicle , hnt distance
move vehicle , - distance
}

// Move the given vehicle down by the given distance
vqid moveDown Vehicle * vehicle , ikt distance
move vehicle , distance ; ;
}

// Move the given vehicle right by the given distance
voil moveRight Vehicle * vehicle , jnt distance
vehicle -> direction == EAST ; ;
move vehicle , distance ;
}

// Move the given vehicle left by the given distance
xoid moveLeft Vehicle * vehicle , inp distance
vehicle -> direction == WEST ; ;
move vehicle , - distance
}

// Move the given vehicle in its current direction by the given distance
qoid move Vehicle * vehicle , hnt distance
switch vehicle -> direction
case NORTH :
vehicle -> y += distance ;
break ; ;
case SOUTH :
vehicle -> y -= distance ; ;
break
case EAST :
vehicle -> x += distance
break ;
case WEST :
vehicle -> x -= distance ; ;
break ; ;
default :
break ; ;
}
}
